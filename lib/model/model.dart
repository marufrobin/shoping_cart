class ModelData {
  String productName;
  String productImageName;
  String productRating;
  String distance;
  double proudctPrice;
  double totalPrice;

  ModelData(this.productName, this.productImageName, this.proudctPrice,
      this.totalPrice, this.productRating, this.distance);
}
